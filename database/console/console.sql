SELECT
    ID,
    TENANT_CODE,
    MEMBER_ID,
    EXPIRES_AT
FROM WKSP_SINDICATTO.PASSWORD_RESET_TOKEN
WHERE TOKEN_HASH = :TOKEN_HASH
    AND ACTIVE = 1
    AND USED_AT IS NULL
    AND EXPIRES_AT > SYSTIMESTAMP;